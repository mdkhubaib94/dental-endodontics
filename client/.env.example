# Optional for production when frontend and API are on different domains
# Example: https://api.yourdomain.com
# Leave empty for same-origin (recommended when deploying this repo as one Vercel project)
VITE_API_BASE_URL=
