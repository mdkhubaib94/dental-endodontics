{
  "name": "srm-dental-web-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build:client": "npm run build --prefix client",
    "build:server:install": "npm install --prefix server",
    "build:client:install": "npm install --prefix client --include=dev",
    "build:vercel": "npm run build:server:install && npm run build:client:install && npm run build:client",
    "build:render": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start:render": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "react-chartjs-2": "^5.3.1",
    "recharts": "^3.7.0"
  }
}
